<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
    <select id="selectByName" parameterType="int" resultType="com.study.Board.DTO.testDTO">
        select name from test_tb where id = #{id}
    </select>
</mapper>